name: cyanweather_webf
description: CyanWeather rebuilt with WebF - HTML/CSS/JS running in Flutter.
publish_to: "none"
version: 1.5.0+6

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webf: ^0.24.27
  geolocator: ^13.0.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/web/
